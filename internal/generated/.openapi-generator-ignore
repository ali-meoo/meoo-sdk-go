# OpenAPI Generator Ignore
# Generated by openapi-generator https://github.com/openapitools/openapi-generator

# Use this file to prevent files from being overwritten by the generator.
# The patterns follow closely to .gitignore or .dockerignore.

# 本文件是 sdks/go/generated/ 内唯一手工维护的忽略文件，作用等价于 Java 的
# sdks/java/.openapi-generator-ignore：go 生成器会顺带产出打包脚本、文档、桩测试和
# 支撑文件（client/configuration/response/utils），这里统一裁剪，只保留真正由契约
# 驱动的 api_*.go 与 model_*.go，以及 .openapi-generator 元数据。
# 生成器不会覆盖已存在的本文件，因此裁剪结果对 check-generated.sh 是确定性的。

# ── 构建与打包：module 根在上一级目录，由手写的 sdks/go/go.mod 独立负责（覆盖
#    generated/ 与 meoo/ 两个包）。生成器默认还会顺带产出 go.sum、git_push.sh、
#    .travis.yml、.gitignore——都与本仓库的构建/CI/VCS 无关，一并排除。──────────
go.mod
go.sum
git_push.sh
.travis.yml
.gitignore

# ── 文档与桩测试：不参与门禁；test/ 会被 `go test ./...` 当作真实测试编译执行 ────
README.md
docs/
test/

# ── 生成器把整份 spec 复制到 api/openapi.yaml。它含全部 operationId，若不排除，
#    verify-operation-coverage.sh 对 sdks/go/generated 的 grep 会命中这份副本而“假
#    通过”（即便真正的 api_*.go 没生成）。契约唯一真源是 spec/，这里排除副本；
#    注意 api/** 只匹配 api/ 目录，不会误伤同目录下 api_ 前缀的生成文件。────────
api/**

# ── 手写静态基础设施（package generated）────────────────────────────────────────
# 这四个文件与契约无关（不随 operationId/schema 变化），却是 HTTP 发起调用、序列化
# 反序列化与通用异常处理的所在，也是生成器缺陷最集中的地方（callAPI 不做超时/重试、
# decode 对 oneOf 的分支、setBody 的 content-type 嗅探、byte body 走 %v 等）。因此
# 整组抽回手写：生成器不再产出它们，改由本目录下手写文件提供生成 api/model 依赖的
# 同包符号面——APIClient / Configuration / APIResponse / GenericOpenAPIError /
# Nullable* / Ptr* / MappedNullable / reportError / parameterAddToHeaderOrQuery /
# selectHeaderContentType / selectHeaderAccept / decode / prepareRequest / callAPI 等。
# 手写实现按职责拆到 client.go（装配）、transport.go（HTTP 发起）、codec.go
# （序列化反序列化）、errors.go（异常）、params.go（参数编码）、configuration.go、
# response.go、utils.go；其中 transport.go/codec.go/errors.go/params.go 是生成器本就
# 不产出的新增文件，无需在此排除。手写文件必须维持上述符号面，偏差会在编译期暴露。
client.go
configuration.go
response.go
utils.go
